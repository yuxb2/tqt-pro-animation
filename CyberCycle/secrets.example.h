#pragma once

// Copy this file to secrets.h, then enter the Wi-Fi network used by the T-QT.
// secrets.h is ignored by Git and must never be committed.
#define OTA_WIFI_SSID "your-wifi-name"
#define OTA_WIFI_PASSWORD "your-wifi-password"
